Per-task assessment + outstanding issues + hashline targets are provided in assistant response (2026-02-28). Key blockers: (1) index.ts new pi.on call will break existing tests/entry-point.test.ts mockPi (missing on), must update test mock; (2) AC14 priority test uses 'cargo test' but isBuildCommand doesn't match it; adjust test command to match both test+build (e.g., 'npm test && npm run build' or 'cargo test && cargo build'); (3) several tasks' Step 2 expected failure messages don't match the actual failures (routing tests will fail due to spy not called, not savedChars); plus minor: plan references AGENTS.md but repo uses AGENT-NATIVE-TOOLS.md.